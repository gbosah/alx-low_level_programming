#include "main.h"
#include <string.h>

/**
 * _strlen - the function that print the length of a strings
 *
 * @s: simple input parameter
 *
 * Return: return (0)
 */

int _strlen(char *s)
{
	int nums;

	nums = strlen(s);
	return (nums);
}

